-----------------------------------
--- Počet '+' Označuje prioritu ---
-----------------------------------

### TODO - CLASS DIAGRAM ###
++++++   TODO 1.1   > (function high)   Přejmenování funkce na existující sice vyhodí hlášku, ale je napsaný nový název
++       TODO 1.2   > (function low)    Při načítání diagramu smazat všechny classy

### TODO - SEQUENCE DIAGRAM ###
++      TODO 2.1   > (design low)       Life-line lze posunout do záporných hodnot (nad Sequence diagram)
++      TODO 2.2   > (design low)       Life-line lze roztáhnout i do záporných hodnot
+++     TODO 2.5   > (design med)       Life-line se občas posouvá i při kliknutí pravým tlačítkem
++      TODO 2.6   > (design low)       Message-line lze posunout do záporných hodnot
+       TODO 2.7   > (design low)       Při posouvání seq diagramů (tedy posouvání všech life-lines po x ose) roztahovat šipky se zprávami k nim nabindovaným, aby měla správnou délku (A řešit změnu typu zpráv, pokud se změní x-pozice)
+++++++ TODO 2.9   > (MUST HAVE)        Načítání seq diagramu ( + nekonzistence po načtení)
+++++++ TODO 2.10  > (MUST HAVE)        Ukládání seq diagramu
+       TODO 2.11  > (design low)       Kvůli offsetu při přidávání se pozdější seq diagramy vykrelují mimo okno (resetovat offset jako u class diagramů)

### TODO - JEDEN SEQUENCE DIAGRAM - NEKONZISTENCE ###
+++++++ TODO 3.2  > (function HIGH)     Zpětná zpráva by neměla být červená nikdy a už vůbec ne oranžová
++++    TODO 3.3  > (function med)      Při přejmenování message-line na funkci, která se nachází ve vyšší třídě (přes generalize) zůstává červená. Budeme muset ještě checkovat, zda se ta metoda nachází nejen v aktuální třídě, ale i ve třídách, které generalizuje
++      TODO 3.6  > (design low)        Koncová část šipky má z nějakýho random důvodu občas špatnou barvu, upřímně nevím, jak se ta barva nastavuje xd

### TODO - VÍCE SEQUENCE DIAGRAMŮ ###
TBD

### TODO - VÍCE SEQUENCE DIAGRAMŮ - NEKONZISTENCE ###
TBD

### DEEP TESTOVÁNÍ - CLASS DIAGRAM ###
> Přidání třídy, posouvání třídy, diagram nelze přesouvat při nevalidní pozici myši.
> Přejmenování třídy (potvrzení enterem), při přejmenování se nenaruší žádná funkcionalita, ošetření přejmenování na existující jméno, prázdné jméno třídy je validní a funguje.
> Přidání atributů, žádné pole nesmí být prázdné, nelze přidat atribut s existujícím jménem, zavření boxu.
> Editace atributu, při nevalidním vstupu, existujícím jméně, nebo zavření boxu se změny neprojeví. Nelze editovat atribut, pokud není žádný vybrán, pokud je vybrán, není nutné kliknout přímo na ten atribut, lze editovat jen jméno, jen typ, jen acces type, nebo nic.
> Přidání funkce, žádné pole nesmí být prázdné, nelze přidat funkci s existujícím jménem, zavření boxu, funkce bez parametrů
> Přidávání parametrů, přidání, či editování parametru funkce s prázdným jménem nepovolí, či datovým typem, nelze přidat parametr s existujícím jménem (při existujícím, se dá název opravit, nebo zavřít křížkem),
> Editace funkce (TODO 1.1), prázdné jméno, prázdný typ (implicitně void), při zruřeší editace se zachová staré jméno a datový typ, lze editovat pouze typ, jméno, nebo nic
> Editace parametrů, editace bez vybrání neudělá nic
> Mazání attributu, třída bez atributu nic nemaže, nevabraný atribut se nesmaže, po smazání je jméno potom znovu dostupné, lze smazat i atribut s primárním klíčem
> Mazání funkce, lze smazat funkci včetně parametrů, lze smazat funkci s Override
> Toggle primary, při nevybraném atributu se nic neděje, lze nastavit a zase zrušit primární klíč, lze přesunout primary na jiný atribut, nelze dát pk na funkci, původní se nezruší, pokud se pokusí nastavit funkci
> Přidávání constraintů, nelze přidat constraint se stejným to a from, nelze přidat více než jeden contstraint na stenou pozici, Add constraint uloží třídu, add constraint znovu změní vybranou třídu, smazání vybrané třídy zruší vybranou třídu
> Editace constraintu, prázdný string je validní název, ničemu nevadí, Kardniality fungují, změny se projevují, pozice šipek je jasná, křížek discardne změny, text se centruje na šipce, constraint lze smazat, při smazání jedné ze tříd se všechny constrainty z nebo do té třídy smažou, 
> Override, Projevuje se při změně typu constraintu na generalizaci, změně zpět na jiný typ, smazání cconstraintu, přejenování jedné i druhé metody, smazání nadřazené funkce, smazání nadřazeného classu
> Načítání ze souboru, skvěle funguje, správné vykreslení, prvky jsou klikatelné, hýbatelné, editovatelné, mazatelné, stejně jako výše, načtou se PK, načte se Override
> Ukládání do souboru a opětovné načtení

### DEEP TESTOVÁNÍ - JEDEN SEQUENCE DIAGRAM ###
> Přidání seq diagramu (TODO 2.11), posouvání seq diagramu, nelze posunout do záporných hodnot, při přejmenování diagramu v class diagramu se přejmenuje i diagram v class diagramu
> Ostraňování seq diagramu, při odstranění diagramu v class diagramu se odstraní i seq diagram (choice box)
> Přidání life-lines, posouvání life-lines (TODO 2.1), posunutí seq diagramu posouvá i všechny jeho life-lines, roztahování life-lines (TODO 2.2)
> Odstraňování life-lines, odstraňování seq diagramu odstraní i jeho life-line (to i from)
> Přejmenování třídy v class diagramu přejmenuje i třídu v seq diagramu
> Přidávání message-lines (TODO 2.5), posouvání message-lines (TODO 2.6)(TODO 2.7)
> Odstraňování message-lines, při odstranění seq diagramu, se odstraní všechny její message-liny
> Načítání a ukládání (TODO 2.9)(TODO 2.10)

### DEEP TESTOVÁNÍ - JEDEN SEQUENCE DIAGRAM - NEKONZISTENCE ###
> Přidání message-line, Přidání neexistující message-line (červená), přidávání zpětné message-line (TODO 3.2)
> Přejmenování message-line (TODO 3.3), přejmenování na existující (červená), přejmenování na neexistující (červená), přejmenování zpětné message-liny (TODO 3.2)
> Smazání diagramu z class diagramu (life-lines i message-lines červené červené)
> Přidání Life-liny smazanému class diagramu (červená)
> Přdání message-line červené life-lině ze všech stran (červená)
> Přejmenování funkce v class diagramu (červená message-line), Přejmenování funkce zpět na existující (černá message-line), smazání funkce (červená), přidání funkce (černá)(TODO 3.6)

### Na čo se nebude hrabat ###
> Nesmí se vytvářet třídy před načítaním ze souboru
> Když se přejemenovává Classa, tak se musí stisknout enter